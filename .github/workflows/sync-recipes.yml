name: Sync Recipes from Finances

on:
  repository_dispatch:
    types: [sync-recipes]

concurrency:
  group: sync-recipes
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Check for duplicate products
        run: uv run pre-commit run check-duplicate-products --all-files

      - name: Write product files from payload
        id: write
        env:
          PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: uv run python sync_products.py

      - name: Close stale sync PRs
        if: steps.write.outputs.has_changes == 'true'
        run: |
          # Find and close any open sync PRs, deleting their branches
          gh pr list \
            --search "sync: update products from recipes in:title" \
            --state open \
            --json number,headRefName \
            --jq '.[] | "\(.number) \(.headRefName)"' \
          | while read -r number branch; do
              echo "Closing stale sync PR #${number} (branch: ${branch})"
              gh pr close "$number" --delete-branch
            done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create PR
        if: steps.write.outputs.has_changes == 'true'
        run: |
          DATE=$(date +%Y-%m-%d)
          BRANCH="sync/recipes-${DATE}-${{ github.run_id }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git checkout -b "$BRANCH"
          git add content/products/
          # Only commit product content — never workflow files
          git restore --staged .github/ 2>/dev/null || true
          git commit -m "sync: update products from recipes ${DATE}"

          # Rebuild site from updated product files
          uv run python build.py
          git add index.html sitemap.xml products/
          git diff --staged --quiet || git commit -m "chore: rebuild site after sync ${DATE}"

          git push origin "$BRANCH"

          BODY=$(cat /tmp/pr-summary.md)
          gh pr create \
            --title "sync: update products from recipes ${DATE}" \
            --body "$BODY" \
            --base main \
            --head "$BRANCH" \
            --reviewer williambdean
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: No changes
        if: steps.write.outputs.has_changes != 'true'
        run: echo "All products are up to date — no PR needed."
